* Укажите домен и порт вебадминки в .env
* Для запуска
``` 
docker-compose up -d
```
* Для подключения к веб админке перейдиет по домену и порту указанному в env
* login: cn=admin,dc=example,dc=org
* password: admin